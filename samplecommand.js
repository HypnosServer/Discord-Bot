const Discord = require('discord.js')
module.exports.run = (client, msg, args, config) =>{
    //command goes in here
}

exports.help = {
    name:"command",
    usage:"command args",
    desc: "What does the command do",
    group: "group?",
    adminOnly: false,
    memberOnly: false,
    example: "commmand seggs"
}